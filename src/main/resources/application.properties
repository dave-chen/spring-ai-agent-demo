# Application config
spring.main.banner-mode=off

# Agent configuration (populated from environment or secrets)
agentcore.runtime-id=${AGENTCORE_RUNTIME_ID:}
agentcore.role-arn=${AGENTCORE_ROLE_ARN:}
agent.aws.region=${AWS_REGION:}
agent.s3.bucket=${AGENT_ARTIFACTS_BUCKET:}
agent.queue.url=${AGENT_QUEUE_URL:}
agent.lock.table=${LOCK_TABLE_NAME:AgentLockTable}

